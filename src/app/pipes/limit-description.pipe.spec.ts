import { LimitDescriptionPipe } from './limit-description.pipe';

describe('LimitDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
